name: compass
channels:
  - conda-forge
  - nodefaults
dependencies:
  # isce3 can be added if forge version is sufficient
  - gdal>=3.0
  - h5py
  - lxml
  - numpy
  - pandas
  - pyproj
  - pytest
  - python>=3.9
  - requests
  - ruamel.yaml
  - scipy
  - shapely
  - yamale
